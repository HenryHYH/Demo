@{
    ViewBag.Title = "Index";
    Layout = "~/Administrator/Views/Shared/_ListLayout.cshtml";
}
@using FW.Web.Framework.UI

<div id="tb-container" class="table-responsive"></div>
@Html.Action("Table", "Common", new { area = "Administrator" })
<script type="text/html" id="tb-tmpl1">
    <table class="table">
        <thead>
            <tr>
                {{each columns as col}}
                <th>{{col.title}}</th>
                {{/each}}
                <th>Operation</th>
            </tr>
        </thead>
        <tbody>
            {{each data as row}}
            <tr>
                <td>{{row.Index}}</td>
                <td>{{row.Name}}</td>
                <td>{{row.Age}}</td>
                <td>Haha</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</script>

@section Script
{
    <script type="text/javascript">
        $(function () {
            $("#tb-container").table({
                url: '@Url.Action("UserList", "User")',
                // template: "tb-tmpl1",
                // customTemplate: true,
                // condition: { pageIndex: 1, pageSize: 5 },
                // pager: { pageIndex: 1, pageSize: 1000 },
                columns: [{ title: "No.", field: "Id" }, { title: "Name", field: "Name" }, { title: "Age", field: "Age" }]
            });
        });
    </script>
}